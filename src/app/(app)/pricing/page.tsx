import { PlaceholderPage } from "@/components/placeholder-page";

export default function PricingPage() {
  return (
    <PlaceholderPage
      title="Pricing"
      description="Use this area for competitor comparisons, historical price views, and future pricing decision support."
      checkpoints={[
        "Add product selector and competitor comparison chart.",
        "Attach data tables for price history and benchmark deltas.",
        "Keep pricing visualizations export-ready for the presentation.",
      ]}
    />
  );
}
